# Legacy Documentation Archive

This folder keeps older project notes that are no longer the primary navigation layer.

Use the current docs first:

- `project_state.md` for the current handoff snapshot
- `docs/project_atlas/index.md` for module ownership and reading order
- `docs/project_atlas/change-routing.md` for likely edit entry points
- `docs/project_history/index.md` for dated snapshots and regression tracing

Archived files may still explain why older workflows looked the way they did, but they can be stale. Do not treat them as the source of truth for current routes, APIs, or data structures.

## Contents

- `literature_screening/architecture.md` — older architecture overview, superseded by `docs/project_atlas/`.
- `literature_screening/web-workbench.md` — older web-workbench overview, superseded by the frontend and workflow module cards in `docs/project_atlas/modules/`.
